{
  "name": "cabana-afrodite-reservas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sistema de reservas diretas da Cabana Afrodite (Netlify Functions + Supabase + Mercado Pago Checkout Pro).",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "build": "tsc --noEmit && echo \"functions são empacotadas pelo Netlify (esbuild)\"",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "mercadopago": "^2.0.15"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.1",
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
